version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: mzansiuser
      POSTGRES_PASSWORD: mzansipass
      POSTGRES_DB: mzansibuilds
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: